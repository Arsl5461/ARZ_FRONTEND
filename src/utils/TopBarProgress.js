import TopBarProgress from 'react-topbar-progress-indicator';

/**
 * Configure and Initialize the TopBarProgress component
 *
 * Author: Muhammad Rooman
 * Date: 15 Sep, 2023
 */

TopBarProgress.config({
    barColors: {
        1: '#078CDD',
        '1.0': '#078CDD',
    },
    shadowBlur: 5,
});

export default TopBarProgress;
